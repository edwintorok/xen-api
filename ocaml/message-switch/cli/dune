(executable
  (modes exe)
  (name main)
  (libraries
    cmdliner
    message_switch_core
    message_switch_unix
    mtime
    rpclib.core
    rpclib.json
    threads.posix
  )
  (preprocess (pps ppx_deriving_rpc))
)

(install
 (package xapi)
 (section sbin)
 (files (main.exe as message-cli))
)
