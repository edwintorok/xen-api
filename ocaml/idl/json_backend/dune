(executable
  (modes exe)
  (name gen_json)
  (libraries    
    fmt
    xapi_datamodel
    xapi-consts
    xapi-stdext-unix
    xapi_stdext_std
    yojson
  )
)

(rule
  (alias runtest)
  (deps
    (:x gen_json.exe)
  )
  (package xapi)
  (action (run %{x}))
)

