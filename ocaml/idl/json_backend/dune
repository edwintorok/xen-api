(executable
  (modes exe)
  (name gen_json)
  (libraries
    dune-build-info
    fmt
    xapi-datamodel
    xapi_consts
    xapi_stdext_unix
    xapi_stdext_std
    yojson
  )
)

(rule
  (alias runtest)
  (deps
    (:x gen_json.exe)
  )
  (package xapi)
  (action (run %{x}))
)

