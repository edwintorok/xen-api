(test
 (name test)
 (modes exe)
 (package xapi)
 (libraries
  alcotest
  cpuid
  
  fmt
  result
  rpclib.core
  rpclib.json
  xapi-idl
  xapi_idl_xen_interface
  xapi_idl_xen_interface_types
  xapi-log
  xapi-stdext-pervasives
  xapi-test-utils
  xapi_xenopsd
  xenstore_transport.unix
 )
 (preprocess
  (per_module ((pps ppx_deriving_rpc) Test))
 )
)

(rule
  (alias runtest)
  (package xapi)
  (deps
    (:x ../lib/xapi_xenopsd.cmxs)
  )
  (action (run ./check-no-xenctrl.sh %{x}))
)

(rule
  (alias runtest)
  (package xapi)
  (deps
    (:x ../simulator/xenops_simulator_main.exe)
  )
  (action (run ./check-no-xenctrl.sh %{x}))
)
